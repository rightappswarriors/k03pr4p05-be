FROM node:22-alpine

# Install Redis
RUN apk add --no-cache redis

WORKDIR /app

COPY package*.json ./
COPY prisma ./prisma/

RUN npm ci --legacy-peer-deps

COPY . .

RUN npm run build

COPY start.sh .
RUN chmod +x start.sh

EXPOSE 4000

CMD ["./start.sh"]